<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qidianit.mes.sys.mapper.PermissionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.qidianit.mes.pojo.SysPermission">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="permission_name" property="permissionName"/>
        <result column="permission_code" property="permissionCode"/>
        <result column="permission_type" property="permissionType"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="icon" property="icon"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_time" property="createdTime"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="updated_time" property="updatedTime"/>
        <result column="remark" property="remark"/>
    </resultMap>

    <!-- 权限树形结构映射 -->
    <resultMap id="PermissionTreeResultMap" type="com.qidianit.mes.pojo.SysPermission" extends="BaseResultMap">
        <collection property="children" ofType="com.qidianit.mes.pojo.SysPermission" select="selectPermissionTree" column="{parentId=id, status=status}"/>
    </resultMap>

    <select id="selectRoleCodesByUserId" resultMap="BaseResultMap">
        SELECT r.role_code FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId} AND r.status = 1
        ORDER BY r.sort ASC
    </select>

    <select id="selectPermissionsByRoleId" resultMap="BaseResultMap">
        SELECT * FROM sys_permission WHERE id IN (
            SELECT permission_id FROM sys_role_permission WHERE role_id = #{roleId}
        )
    </select>

    <!-- 权限分页查询 -->
    <select id="selectPermissionPage" resultMap="BaseResultMap">
        SELECT * FROM sys_permission
        <where>
            <if test="permission.permissionName != null and permission.permissionName != ''">
                AND permission_name LIKE CONCAT('%', #{permission.permissionName}, '%')
            </if>
            <if test="permission.permissionCode != null and permission.permissionCode != ''">
                AND permission_code LIKE CONCAT('%', #{permission.permissionCode}, '%')
            </if>
            <if test="permission.permissionType != null">
                AND permission_type = #{permission.permissionType}
            </if>
            <if test="permission.parentId != null">
                AND parent_id = #{permission.parentId}
            </if>
            <if test="permission.status != null">
                AND status = #{permission.status}
            </if>
        </where>
        ORDER BY sort ASC, created_time DESC
    </select>

    <!-- 根据权限编码查询权限 -->
    <select id="selectByPermissionCode" resultMap="BaseResultMap">
        SELECT * FROM sys_permission WHERE permission_code = #{permissionCode}
    </select>

    <!-- 查询权限树形结构 -->
    <select id="selectPermissionTree" resultMap="PermissionTreeResultMap">
        SELECT * FROM sys_permission
        WHERE parent_id = #{parentId}
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY sort ASC
    </select>

    <!-- 根据权限ID查询角色ID列表 -->
    <select id="selectRoleIdsByPermissionId" resultType="java.lang.Long">
        SELECT role_id FROM sys_role_permission WHERE permission_id = #{permissionId}
    </select>

    <!-- 根据用户ID查询权限列表 -->
    <select id="selectPermissionsByUserId" resultMap="BaseResultMap">
        SELECT DISTINCT p.* FROM sys_permission p
                                     INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
                                     INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND p.status = 1
        ORDER BY p.sort ASC
    </select>

    <!-- 根据用户ID查询权限编码列表 -->
    <select id="selectPermissionCodesByUserId" resultType="java.lang.String">
        SELECT DISTINCT p.permission_code
        FROM sys_permission p
                 INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
                 INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND p.status = 1
    </select>
</mapper>
