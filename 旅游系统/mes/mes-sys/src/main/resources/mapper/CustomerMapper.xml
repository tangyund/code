<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qidianit.mes.sys.mapper.CustomerMapper">
    <resultMap id="BaseMap" type="customer">
        <id property="id" column="id"/>
        <result property="status" column="status"/>
        <result property="deleteFlag" column="delete_flag"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="name" column="name"/>
        <result property="address" column="address"/>
        <result property="code" column="code"/>
        <result property="contactPerson" column="contact_person"/>
        <result property="description" column="description"/>
        <result property="email" column="email"/>
        <result property="fax" column="fax"/>
        <result property="phone" column="phone"/>
    </resultMap>
    <update id="updateById" parameterType="customer">
        update customer  set update_time = now()
        <if test="name != null and name != ''">
            ,name = #{name}
        </if>
        <if test="address != null and address != ''">
            ,address = #{address}
        </if>

        <if test="contactPerson != null and contactPerson != ''">
            ,contact_person = #{contactPerson}
        </if>
        <if test="phone != null and phone != ''">
            ,phone = #{phone}
        </if>
        <if test="fax != null and fax != ''">
            ,fax = #{fax}
        </if>
        <if test="email != null and email != ''">
            ,email = #{email}
        </if>
        <if test="description != null and description != ''">
            ,description = #{description}
        </if>
        <if test="status != null">
            ,status = #{status}
        </if>
        where id = #{id}
    </update>
    <update id="deleteById" parameterType="long">
        update customer set delete_flag = 1 where id = #{id}
    </update>
    <update id="batchDelete">
        update customer set delete_flag = 1
        where id in 
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </update>

    <select id="selectList" resultMap="BaseMap">
        SELECT * FROM customer WHERE delete_flag != 1
        <if test="code != null and code != ''">
            AND code = #{code}
        </if>
        <if test="name != null and name != ''">
            AND name = #{name}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>
    <select id="selectByCode" resultMap="BaseMap">
        SELECT * FROM customer WHERE code = #{code}
    </select>


    <insert useGeneratedKeys="true" keyProperty="id" keyColumn="id" id="saveCustomer" parameterType="customer">
        insert customer(code,name,address,contact_person,phone,fax,email,description)
            values(#{code},#{name},#{address},#{contactPerson},#{phone},#{fax},#{email},#{description})
    </insert>
</mapper>
